///note_init();

// A ds_list with all the TARGET times of each note
global.notes_ds_time = ds_list_create();

// You can create other lists like:
// (this is the image_index of the note)
 global.notes_ds_type = ds_list_create();
// And add the type of note,
// or sprite_index
// or colour, or score, 
// or target position, or other values for each note

 global.notes_ds_duration = ds_list_create();


